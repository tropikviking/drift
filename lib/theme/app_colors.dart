import 'package:flutter/material.dart';

/// Centralized color definitions for the Drift App
///
/// All application colors should be defined here to ensure
/// consistency and make future branding updates simple

class AppColors {
  AppColors._();

  static const Color oceanBlue = Color(0xFF0A3D62);
  static const Color seaBlue = Color(0xFF1E6F8C);
  static const Color seaFoam = Color(0xFF5BC0BE);
  static const Color sand = Color(0xFFF8FAFC);
  static const Color white = Color(0xFFFFFFFF);
  static const Color charcoal = Color(0xFF1F2937);
}